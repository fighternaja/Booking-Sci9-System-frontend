# ระบบจองห้องประชุม - ปฏิทินการจอง

## คุณสมบัติใหม่

### 🗓️ ปฏิทินการจองแบบ Interactive
- **ปฏิทินแบบเต็มรูปแบบ**: แสดงการจองทั้งหมดในรูปแบบปฏิทินที่สวยงาม
- **การเลือกวันที่แบบง่าย**: คลิกที่ช่องว่างในปฏิทินเพื่อจองห้อง
- **การดูรายละเอียด**: คลิกที่การจองเพื่อดูข้อมูลรายละเอียด
- **การนำทาง**: ใช้ปุ่มนำทางเพื่อเปลี่ยนเดือน/สัปดาห์/วัน

### 🎨 UI/UX ที่ปรับปรุง
- **Popup Modal**: แสดงข้อมูลการจองในรูปแบบ popup ที่สวยงาม
- **สีสันที่แตกต่าง**: การจองในอนาคต (สีน้ำเงิน), ปัจจุบัน (สีส้ม), อดีต (สีเทา)
- **Responsive Design**: รองรับการใช้งานบนมือถือและแท็บเล็ต
- **คำแนะนำการใช้งาน**: แสดงวิธีใช้งานในหน้า booking

### 🔧 ฟีเจอร์การทำงาน
- **ตรวจสอบความพร้อมอัตโนมัติ**: ตรวจสอบว่าห้องว่างหรือไม่ก่อนจอง
- **การตั้งค่าเวลาเริ่มต้น**: ตั้งเวลาเริ่มต้นเป็นชั่วโมงถัดไปโดยอัตโนมัติ
- **การแสดงสถานะ**: แสดงสถานะการจองแบบเรียลไทม์
- **การจัดการข้อผิดพลาด**: แสดงข้อความข้อผิดพลาดที่เข้าใจง่าย

## วิธีการใช้งาน

### สำหรับผู้ใช้ทั่วไป
1. เข้าสู่หน้า **ห้องทั้งหมด** (`/rooms`)
2. เลือกห้องที่ต้องการจอง
3. คลิกปุ่ม **"จองห้อง"**
4. ในหน้า booking ใหม่:
   - ดูข้อมูลห้องทางด้านซ้าย
   - คลิกที่ช่องว่างในปฏิทินเพื่อจอง
   - กรอกข้อมูลการจองใน popup
   - ยืนยันการจอง

### สำหรับการดูการจอง
1. คลิกที่การจองในปฏิทิน
2. ดูรายละเอียดการจองใน popup
3. ปิด popup เมื่อดูเสร็จ

## โครงสร้างไฟล์

```
frontend/app/
├── components/
│   ├── BookingCalendar.js      # คอมโพเนนต์ปฏิทินหลัก
│   ├── BookingModal.js         # Popup สำหรับการจอง
│   └── CalendarStyles.css     # สไตล์สำหรับปฏิทิน
└── rooms/[id]/book/
    └── page.js                 # หน้า booking ที่อัพเดทแล้ว
```

## การติดตั้ง Dependencies

```bash
npm install moment
```

## การใช้งาน API

### ดึงข้อมูลการจองตามช่วงเวลา
```javascript
GET /api/rooms/{room_id}/bookings?start_date=YYYY-MM-DD&end_date=YYYY-MM-DD
```

### ตรวจสอบความพร้อมของห้อง
```javascript
POST /api/rooms/{room_id}/check-availability
{
  "start_time": "2024-01-01T09:00:00",
  "end_time": "2024-01-01T10:00:00"
}
```

## การปรับแต่ง

### เปลี่ยนสีของปฏิทิน
แก้ไขไฟล์ `CalendarStyles.css`:
```css
.rbc-event {
  background-color: #your-color;
}
```

### เปลี่ยนภาษา
แก้ไขไฟล์ `BookingCalendar.js`:
```javascript
moment.locale('en'); // เปลี่ยนเป็นภาษาอังกฤษ
```

## การแก้ไขปัญหา

### ปฏิทินไม่แสดง
- ตรวจสอบว่า moment.js ติดตั้งแล้ว
- ตรวจสอบการ import CSS

### Popup ไม่ทำงาน
- ตรวจสอบว่า react-modal ติดตั้งแล้ว
- ตรวจสอบการตั้งค่า z-index

### การจองไม่สำเร็จ
- ตรวจสอบการเชื่อมต่อ API
- ตรวจสอบการส่ง token authentication

## การพัฒนาต่อ

### ฟีเจอร์ที่สามารถเพิ่มได้
- **การจองแบบ recurring**: จองซ้ำทุกสัปดาห์/เดือน
- **การแจ้งเตือน**: แจ้งเตือนก่อนการจอง
- **การ export**: ส่งออกข้อมูลการจองเป็น PDF/Excel
- **การจัดการทรัพยากร**: จองอุปกรณ์เพิ่มเติม
- **การชำระเงิน**: เชื่อมต่อระบบชำระเงิน

### การปรับปรุงประสิทธิภาพ
- **Lazy loading**: โหลดข้อมูลการจองตามต้องการ
- **Caching**: เก็บข้อมูลการจองใน cache
- **Pagination**: แบ่งหน้าข้อมูลการจอง
- **Real-time updates**: อัพเดทข้อมูลแบบเรียลไทม์
